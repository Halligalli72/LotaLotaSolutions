export class Participant {
    constructor(
        participantId?: string,
        tourId?: number,
        name?: string,
        alias?: string,
        isCompeting?: boolean,
        isTourAdmin?: boolean,
        isTourOfficial?: boolean,
        totalPoints?: number,
        totalRank?: number,
        created?: Date,
        createdBy?: string,
        updated?: Date,
        updatedBy?: string
    ) { }
}
