export class Tour {
    constructor(
        tourId?: number,
        name?: string,
        description?: string,
        isPublic?: boolean,
        startDate?: Date,
        endDate?: Date,
        created?: Date,
        createdBy?: string,
        updated?: Date,
        updatedBy?: string
    ) { }
}
